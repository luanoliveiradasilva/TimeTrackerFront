class TimeTracker {
    constructor(timeDate, startTime, breakTime, clockIn, clockOut) {
        this.timeData = timeDate;
        this.startTime = startTime;
        this.breakTime = breakTime;
        this.clockIn = clockIn;
        this.clockOut = clockOut;
    }
}

export default TimeTracker;